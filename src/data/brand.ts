/** Drop your files at these paths — no other site images are used. */
export const BRAND = {
  /** Replace with your file when ready */
  logo: "/logo.png",
  logoFallback: "/logo.svg",
  favicon: "/favicon.ico",
} as const;
